import React from 'react'
import { useHistory } from 'react-router-dom';

function Home() {
const history = useHistory();

  return (
    <div>
    
    {/* <button onClick={() => history.push('/chatbot')}>
      Click me
    </button> */}

    </div>
  )
}

export default Home